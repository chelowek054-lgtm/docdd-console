/**
 * Импорт файла как строки: `?raw` умеет Vite, но типов для него нет.
 * Нужен, чтобы инструкция на экране бралась из самого документа репозитория,
 * а не переписывалась в разметке (docs/04-ui.md).
 */
declare module '*.md?raw' {
  const content: string;
  export default content;
}
