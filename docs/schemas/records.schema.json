{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://docdd/records.schema.json",
  "title": "Предложенные записи",
  "description": "Блок ```docdd-records в ответе модели на разбор входящего. Не файлы, а список: заводит записи приложение.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "records"
  ],
  "properties": {
    "records": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/record"
      }
    }
  },
  "$defs": {
    "record": {
      "type": "object",
      "required": [
        "key",
        "type",
        "title"
      ],
      "additionalProperties": false,
      "properties": {
        "key": {
          "type": "string",
          "minLength": 1,
          "description": "Имя внутри ответа, по которому на запись ссылаются соседние. Номер выдаёт приложение, и модель его не знает."
        },
        "type": {
          "enum": [
            "requirement",
            "design",
            "decision",
            "contract",
            "task",
            "phase",
            "verification",
            "map"
          ]
        },
        "title": {
          "type": "string",
          "minLength": 1
        },
        "body": {
          "type": "string",
          "description": "Тело записи: зачем это, что делаем, чего не делаем, как понять, что готово."
        },
        "change": {
          "enum": [
            "feature",
            "fix",
            "rename",
            "format"
          ],
          "description": "Что за изменение вносит задача."
        },
        "links": {
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "description": "Связи: значение — либо ключ соседней предложенной записи, либо номер уже заведённой."
        },
        "notes": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Заметки, из которых взялась запись: уходят в журнал. Одна запись может опираться сразу на несколько, если знание было разбросано по нескольким файлам. Приложение сверяет каждую со списком разбираемых и чужой текст в журнал не пускает."
        },
        "capabilities": {
          "type": "array",
          "description": "Только у type: map — возможности, которые запись добавляет в функциональную карту. Уходят блоком ```docdd-functional; свидетельств (путь, строка) не требуют — карта подтверждается человеком, а не сверкой с кодом.",
          "items": {
            "type": "object",
            "required": [
              "id"
            ],
            "additionalProperties": false,
            "properties": {
              "id": {
                "type": "string",
                "minLength": 1
              },
              "title": {
                "type": "string"
              },
              "parent": {
                "type": "string",
                "description": "id родительской возможности внутри того же списка — так возможности складываются в дерево."
              }
            }
          }
        }
      }
    }
  }
}
